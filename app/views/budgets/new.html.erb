<div class="row justify-content-center">
  <div class="col-md-12 col-lg-6">
    <div class="card mt-3">
      <div class="card-header">
        <h3><%= t('.head') %></h3>
      </div>

      <div class="card-body">
        <%= render partial: 'form', object: @budgets_facade.new_budget, as: 'budget' %>
      </div>
    </div>
  </div>
</div>

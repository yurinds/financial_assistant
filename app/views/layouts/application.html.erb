<!DOCTYPE html>
<html>
  <head>
    <title>MyFinancialAssistant</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <%# Шапка %>
    <div class=container>
      <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href=<%= root_path %>>Мой финансовый помощник</a>
          </li>
        </ul>

        <% if user_signed_in? %>
          <%= link_to 'Выйти', destroy_user_session_path, class: 'btn btn-outline-danger my-2 my-sm-0', method: :delete %>
        <% else %>
          <%= link_to 'Регистрация', new_user_registration_path, class: 'btn btn-outline-success my-2 my-sm-0' %>
        <% end %>
      </div>
    </nav>
  </div>

  <%# Тело сайта %>
  <div class=container>
    <%= bootstrap_flash %>

    <%= yield %>
  </div>

</body>
</html>

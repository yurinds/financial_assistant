<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MyFinancialAssistant</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <%# Шапка %>
    <div class=container>
      <nav class="navbar navbar-expand-md navbar-dark bg-dark">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <a class="nav-link" href=<%= root_path %>>Мой финансовый помощник</a>
          </li>
        </ul>

        <% if user_signed_in? %>
          <%= link_to 'Создать бюджет', new_budget_path, class: 'btn btn-outline-warning my-2 my-sm-0 ml-2' %>
          <%= link_to 'Категории', categories_path, class: 'btn btn-outline-info my-2 my-sm-0 ml-2' %>
          <%= link_to 'Выйти', destroy_user_session_path, class: 'btn btn-outline-danger my-2 my-sm-0 ml-2', method: :delete %>
        <% else %>
          <%= link_to 'Регистрация', new_user_registration_path, class: 'btn btn-outline-success my-2 my-sm-0' %>
        <% end %>
      </div>
    </nav>
  </div>

  <%# Тело сайта %>
  <div class=container id="main-container">
    <%= bootstrap_flash %>

    <%= yield %>
  </div>

</body>
</html>

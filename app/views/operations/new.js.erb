$(".alert.alert-danger.alert-dismissible").remove();

$("#main-container").prepend("<%= j(render partial: 'partials/error', locals: {resource: @new_operation}) %>");

$("#collapseExample .card-body").html("<%= j(render partial: 'operations/form', 
                                            locals: {budget: @budget, 
                                                    operation: @new_operation, 
                                                    categories: @categories}) %>");

$("#collapseExample").collapse('show');

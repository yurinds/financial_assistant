<%= render partial: 'partials/flash', object: @operation, as: 'resource' %>

$("#collapseExample .card-body").html("<%= j(render partial: 'operations/form', 
                                            locals: {budget: @budget, 
                                                    operation: @operation, 
                                                    categories: @categories,
                                                    payment_methods: @payment_methods}) %>");

$("#collapseExample").collapse('show');

<%= render partial: 'partials/flash', object: @operations_facade.operation, as: 'resource' %>

$("#collapseExample .card-body").html("<%= j(render partial: 'operations/form', object: @operations_facade, as: 'operations_facade' ) %>");

$("#collapseExample").collapse('show');
